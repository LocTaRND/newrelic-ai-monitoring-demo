# Internal service URLs (used by Node.js server)
INTERNAL_USER_API=http://backend-api/api/v1/users
AI_CHATBOT_URL=http://ai-chatbot-service:80/chat

# React app uses relative URLs (/api/v1/users) which proxy through Node.js server